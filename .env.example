# Temporary prototype admin access. Use long, unrelated random values.
ADMIN_PASSWORD=replace-with-a-long-random-password
AUTH_SECRET=replace-with-a-different-long-random-secret

# Added automatically when an Upstash Redis integration is connected in Vercel.
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Optional: enables the live AI practice bot. Without this, the lab falls back to simulator mode.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
